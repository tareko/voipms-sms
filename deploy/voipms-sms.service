[Unit]
Description=voip.ms SMS local backend (UI + API)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
# Path to the project. Adjust if you cloned elsewhere.
WorkingDirectory=%h/workspace/voipms-frontend/server
# .env is loaded by the app via dotenv (config.ts), so no EnvironmentFile here
# (systemd's strict parser can choke on password values).
ExecStart=%h/workspace/voipms-frontend/node_modules/.bin/tsx src/index.ts
Restart=on-failure
RestartSec=5

# Hardening
NoNewPrivileges=true
ProtectSystem=full
ProtectHome=read-only
ReadWritePaths=%h/workspace/voipms-frontend/data %h/workspace/voipms-frontend/web/dist
PrivateTmp=true

[Install]
WantedBy=default.target
